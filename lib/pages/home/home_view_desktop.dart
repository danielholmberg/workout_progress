part of 'home_view.dart';

class _HomeViewDesktop extends StatelessWidget {
  const _HomeViewDesktop({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
      child: Center(
        child: Text('[Desktop] A template Flutter project'),
      ),
    );
  }
}