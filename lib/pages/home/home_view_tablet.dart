part of 'home_view.dart';

class _HomeViewTablet extends StatelessWidget {
  const _HomeViewTablet({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
      child: Center(
        child: Text('[Tablet] A template Flutter project'),
      ),
    );
  }
}