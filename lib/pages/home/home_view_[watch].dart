part of 'home_view.dart';

class _HomeViewWatch extends StatelessWidget {
  const _HomeViewWatch({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {    
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('[Watch] A template Flutter project'),
        ),
      ),
    );
  }
}